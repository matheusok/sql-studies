SELECT *
FROM Person.Person;

SELECT Title
FROM Person.Person;

SELECT *
FROM Person.EmailAddress;

/*
The Marketing team needs to do a search
on the most common names of its customers
and needs the first and last names of all customers
who are registered in the system.
*/
SELECT FirstName, LastName
FROM Person.Person;